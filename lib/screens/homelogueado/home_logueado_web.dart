import 'package:flutter/material.dart';

class HomeLogeuadoWebScreen extends StatelessWidget {
  const HomeLogeuadoWebScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('HomeLogeuadoWebScreen'),
      ),
    );
  }
}
